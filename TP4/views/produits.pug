extends layout

block content
    section.sidebar(aria-label='Filtres')
        section
            h2 Catégories
            #product-categories.btn-group.vertical
                button(data-category='cameras') Appareils photo
                button(data-category='consoles') Consoles
                button(data-category='screens') Écrans
                button(data-category='computers') Ordinateurs
                button(data-category='all').selected Tous les produits
        section
            h2 Classement
            #product-criteria.btn-group.vertical
                button(data-criteria='price-asc').selected Prix (bas-haut)
                button(data-criteria='price-dsc') Prix (haut-bas)
                button(data-criteria='alpha-asc') Nom (A-Z)
                button(data-criteria='alpha-dsc') Nom (Z-A)
    article
        span#products-count.right-header 13 produits
        h1 Produits
        #products-list.products
            each prod in products
                .product(data-product-id=prod.id)
                    a(href='./produits/' + prod.id, title='En savoir plus...')
                        h2 #{prod.name}
                        img(alt='product', src='./assets/img/' + prod.image)
                        p.price
                            small Prix
                            |  #{prod.price}

block customscript
    script(type='text/javascript', src='./assets/scripts/products.controller.js')
